<?xml version="1.0" encoding="utf-8"?>
<!--Chamada de Telas-->

<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <fragment
        android:id="@+id/nav_host_fragment_content_main"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:defaultNavHost="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navGraph="@navigation/nav_graph" />

    <EditText
        android:id="@+id/txtBlocoAnotacao"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:ems="15"
        android:gravity="start|top"
        android:hint='Anote para nao esquecer!'
        android:inputType="textMultiLine"
        android:singleLine="false"
        />

    <!-- ems define a largura dos componentes com relação a fonte atual
        gravity- indica de onde o conteúdo iniciará
        inputType- Teclado comletras e números
        singleLine- quebra de linha, não linhas simples
        layout_constraintEnd e Top - Indica como oconteúdo será distribuído
        no aplicativo. Área que receberá as anotações.
    -->




</android.support.constraint.ConstraintLayout>